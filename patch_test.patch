--- internal/installer/installer_test.go
+++ internal/installer/installer_test.go
@@ -433,7 +433,9 @@

 func TestPacmanAddRepositories(t *testing.T) {
	pacman := &Pacman{}
-	err := pacman.AddRepositories(false, false, []config.Repository{})
+	repos := []config.Repository{
+		{Name: "dummy-repo"},
+	}
+	err := pacman.AddRepositories(false, false, repos)
	if err != nil {
		t.Fatalf("AddRepositories failed: %v", err)
	}
 }
